# =============================================================================
# KTND-Finance: Univariate Configuration
# =============================================================================
# Single-asset analysis using SPY.
# Inherits all defaults from default.yaml.
# =============================================================================

defaults:
  - default

model:
  n_modes: 8
  hidden_dims: [64, 64, 32]

training:
  batch_size: 256

data:
  tickers: ['SPY']
  input_dim: 1
