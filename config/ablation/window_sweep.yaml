# =============================================================================
# Ablation A7: Rolling Window Size Sweep
# =============================================================================
# Sweeps over different rolling window sizes for non-stationarity analysis.
# Inherits all defaults from default.yaml.
# =============================================================================

defaults:
  - ../default

sweep:
  parameter: rolling.window_size
  values: [250, 500, 750, 1000]
